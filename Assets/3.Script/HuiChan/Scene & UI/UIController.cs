using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIController : MonoBehaviour
{
    /*
     목표: 각각 Scene에서의 일할 UI들을 먼저 저장하고 각 Scene에 맞는 UI들을 표시한다. 
     bool 값으로 판단하여 그 Scene에 맞는 UI 활성화

    이 Controller는 UI 정보를 가지고 있음.
    싱글톤 형태. OnEnable로 각 Scene을 판단한다.
     */
}
